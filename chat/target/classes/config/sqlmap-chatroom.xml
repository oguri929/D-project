<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="roomDao">
	<select id="list" resultType="roomVO">
		select * from chatroom order by code
	</select>
	
	<select id="select" parameterType="int" resultType="roomVO">
		select * from chatroom where code = #{code}
	</select>
	
	<insert id="insert" parameterType="roomVO">
		insert into chatroom
		(code,password,local,roomname,roomdiscript,memberlimit,regdate,captain,subjectnum)
		values
		(chatroom_seq.nextval,#{password},#{local},#{roomname},#{roomdiscript},#{memberlimit},SYSDATE,
		#{captain}, #{subjectnum})
		<selectKey keyProperty="code" resultType="Integer">
			select nvl(max(code),0) from chatroom
		</selectKey>
	</insert>
	
	<update id="update" parameterType="roomVO">
		update chatroom 
		set password = #{password}, roomname = #{roomname}, roomdiscript = #{roomdiscript}, local = #{local}
		where code = #{code} and password = #{password}
	</update>
	
	<delete id="delete" parameterType="roomVO">
		delete from chatroom where code = #{code} and password = #{password}
	</delete>
</mapper>